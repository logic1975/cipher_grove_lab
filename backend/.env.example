# Database Configuration (Prisma format)
DATABASE_URL="postgresql://music_label_user:password@localhost:5432/music_label_dev"
DATABASE_URL_TEST="postgresql://music_label_user:password@localhost:5432/music_label_test"

# Server Configuration
NODE_ENV=development
PORT=3000

# Legacy Database Configuration (for backwards compatibility)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=music_label_dev
DB_USER=music_label_user
DB_PASSWORD=password

# Security Configuration
JWT_SECRET=your-super-secret-jwt-key-here
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Email Configuration (future use)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

# File Upload Configuration (future use)
MAX_FILE_SIZE=5000000
UPLOAD_DIR=uploads/

# API Configuration
API_VERSION=v1
API_BASE_URL=http://localhost:3000/api